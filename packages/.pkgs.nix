# this file is for all packages installed using nixInstall
# it is included in the imports on configuraition.nix
# this is updated automatically by nit, it defines package paths
# if you run into any issues please submit a bug report / pr 

{ config, lib, pkgs, ... }:

{
  imports = [

  ];
}
